{
  "name": "worx-search",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler  deploy",
    "monitor": "wrangler tail worx-search",
    "clear-cache": "curl -s \"https://worx-search.worxbranding.workers.dev/admin/clear-cache?site=worxbranding-dev\" \\\n  -H \"x-api-key: dev-secret-123\" | jq .",
    "test:isNoAnswer": "rm -rf dist-tests && ./node_modules/.bin/tsc --module commonjs --target es2020 --types node --skipLibCheck --outDir dist-tests test/isNoAnswer.test.ts && node dist-tests/test/isNoAnswer.test.js && rm -rf dist-tests"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251011.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.43.0"
  }
}
